{
  "name": "gameeval-qa-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "stagehand": "latest"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
